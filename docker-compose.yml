services:
  app:
    build: .
    image: my-mcp-app:1.0.0
    ports:
      - "5000:5000"  # FastAPI app port
      - "5678:5678"  # Debugpy port
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
